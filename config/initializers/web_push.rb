# Web Push VAPID configuration
# Keys are set via environment variables in production

# Nothing to configure at boot time - VAPID keys are passed
# directly in PushNotificationJob when sending notifications
